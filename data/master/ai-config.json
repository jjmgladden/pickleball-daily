{
  "workerBaseUrl": "https://pickleball-daily-api.jjmgladden.workers.dev",
  "aiEnabled": true,
  "note": "Worker deployed 2026-04-27 (Session 8). To disable Ask tab without redeploying, set aiEnabled to false here and commit. To rotate the Worker URL after a re-deploy, update workerBaseUrl. The Worker also reads its own env.AI_DISABLED kill switch (wrangler.toml) — that one disables at the server side, this one disables at the browser side."
}
